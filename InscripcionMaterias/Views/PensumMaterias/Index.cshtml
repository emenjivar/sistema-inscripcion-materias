@model IEnumerable<InscripcionMaterias.Models.PensumMateria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CicloCurricular)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMateriaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMateriaPrerequisitoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPensumNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CicloCurricular)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMateriaNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMateriaPrerequisitoNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPensumNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
